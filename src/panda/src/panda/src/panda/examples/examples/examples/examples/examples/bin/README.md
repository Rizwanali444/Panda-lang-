# Panda 🐼 Language

Panda is a beginner‑friendly programming language created by Rizwan Ali.  
It makes coding fun with simple commands like `SAY`, `COLOR`, `ADD`, `HTTPGET`.

## 🚀 Run Panda
```bash
python -m panda examples/hello.pd
